STM32F4有三种定时器，高级、通用、基本定时器

STM32F40x系列总共最多有14个定时器

通用定时器可以用来PWM输出，基本定时器主要用于驱动ADC

通用定时器TIM2/TIM5为32位定时器，TIM3/TIME4为16位定时器，STM32的每个通用定时器都是完全独立的，没有互相共享的任何资源

4个独立通道(TIMx_CH1~4)，这些通道可以用来作为
    输入捕获
    输出比较
    PWM生成
    单脉冲输出